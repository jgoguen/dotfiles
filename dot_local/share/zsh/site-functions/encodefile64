# vim: set filetype=zsh:
# Do not edit this file directly! It is managed by chezmoi.
emulate -L zsh

if [ $# -eq 0 ]; then
	printf 'You must provide a filename\n' >&2
else
	base64 -i "${1}" -o "${1}.b64"
	printf '%s encoded in Base64 and saved to %s.b64\n' "${1}" "${1}"
fi
